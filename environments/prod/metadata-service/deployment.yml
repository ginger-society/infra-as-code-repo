apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-service-api-deployment
  labels:
    app: metadata-service-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metadata-service-api
  template:
    metadata:
      labels:
        app: metadata-service-api
    spec:
      initContainers:
        - name: wait-for-db
          image: postgres:latest
          command:
            - sh
            - -c
            - |
              until pg_isready -h pg-postgresql -p 5432 -U postgres; do 
                echo "Waiting for DB..."
                sleep 5
              done

      containers:
        - name: metadata-service-api
          image: gingersociety/metadata-service-api-stage:latest
          ports:
            - containerPort: 8000

          env:
            # ✅ DATABASE_URL from secret
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: metadata-service-api-secrets
                  key: DATABASE_URL

            # ✅ Redis (unchanged)
            - name: REDIS_URI
              value: "redis://redis-service:6379"

            # ✅ App secrets from same secret
            - name: ISC_SECRET
              valueFrom:
                secretKeyRef:
                  name: metadata-service-api-secrets
                  key: ISC_SECRET

            - name: ROCKET_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: metadata-service-api-secrets
                  key: ROCKET_SECRET_KEY

            # ✅ Global secret
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: JWT_SECRET

            # ✅ Static config
            - name: ROCKET_ADDRESS
              value: "0.0.0.0"

            - name: ROCKET_PORT
              value: "8000"