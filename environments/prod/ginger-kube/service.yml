apiVersion: v1
kind: Service
metadata:
  name: ginger-kube-service
  labels:
    app: ginger-kube
spec:
  selector:
    app: ginger-kube
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
