apiVersion: v1
kind: Service
metadata:
  name: schema-gen-service-service
  labels:
    app: schema-gen-service
spec:
  selector:
    app: schema-gen-service
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 8000