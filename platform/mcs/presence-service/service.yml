apiVersion: v1
kind: Service
metadata:
  name: presence-service
  labels:
    app: presence-service
spec:
  selector:
    app: presence-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
