apiVersion: v1
kind: Service
metadata:
  name: iam-admin-fe-service
  labels:
    app: iam-admin-fe
spec:
  selector:
    app: iam-admin-fe
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 80