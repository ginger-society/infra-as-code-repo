--web.enable-lifecycle — lets you reload config without restarting the pod: curl -X POST http://prometheus-service/-/reload after editing the ConfigMap and kubectl applying it (ConfigMap changes don't auto-propagate into the running config file otherwise; you'd still need to bounce the pod or hit this endpoint once the volume-mounted file updates, which can take up to ~60s via kubelet sync).