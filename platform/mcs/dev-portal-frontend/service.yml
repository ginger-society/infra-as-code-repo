apiVersion: v1
kind: Service
metadata:
  name: dev-portal-service
  labels:
    app: dev-portal
spec:
  selector:
    app: dev-portal
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 80