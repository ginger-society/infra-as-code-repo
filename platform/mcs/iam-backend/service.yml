apiVersion: v1
kind: Service
metadata:
  name: iamservice-service
  labels:
    app: iamservice
spec:
  selector:
    app: iamservice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
