apiVersion: v1
kind: Secret
metadata:
  name: s3-credentials
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: {{NOTIFICATION.AWS_ACCESS_KEY_ID}}
  AWS_SECRET_ACCESS_KEY: {{NOTIFICATION.AWS_SECRET_ACCESS_KEY}}
  RESTIC_PASSWORD: {{BUCKET.RESTIC_PASSWORD}}